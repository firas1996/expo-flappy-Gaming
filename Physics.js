import Matter from "matter-js";
import Vars from "./Vars";
const Physics = (entities, { touches, time }) => {
  let engine = entities.physics.engine;
  let world = entities.physics.world;
  let bird = entities.physics.bird;
};
